{
  "name": "chrome-site-blocker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "npx @biomejs/biome lint --write ./src",
    "format": "npx @biomejs/biome format --write ./src",
    "build:popup": "vite build",
    "build:content": "cp -r src/content dist/content",
    "build:manifest": "cp manifest.json dist/manifest.json",
    "build": "npm run build:popup && npm run build:content && npm run build:manifest"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "packageManager": "pnpm@10.12.1",
  "dependencies": {
    "@biomejs/biome": "2.1.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "@types/react-dom": "^19.1.6",
    "@vitejs/plugin-react": "^4.7.0",
    "typescript": "^5.8.3",
    "vite": "^7.0.6"
  }
}
